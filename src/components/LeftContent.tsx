
import { Box } from "@mui/system";

function LeftContent(){
    return(
        <Box bgcolor={'lightBlue'} flex={1} sx={{maxHeight:'none'}}>
            Left
        </Box>
    );
    }
    export default LeftContent;